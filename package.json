{
  "name": "catan",
  "private": true,
  "version": "0.0.0",
  "description": "Stable, real-time online Catan replacement — see SPEC.md",
  "scripts": {
    "test": "pnpm -r --if-present run test",
    "typecheck": "pnpm -r --if-present run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
